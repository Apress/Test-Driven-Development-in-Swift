name: Albertos
options:
  bundleIdPrefix: com.mokacoding.Albertos
settings:
  GENERATE_INFOPLIST_FILE: YES
  # The two version values below are necessary, otherwise the generated Info.plist won't be valid
  MARKETING_VERSION: 1.0
  CURRENT_PROJECT_VERSION: 0
